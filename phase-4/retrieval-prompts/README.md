# Retrieval Prompts

This directory stores query-time GraphRAG retrieval outputs generated by:

- `POST /api/graphrag/query`

Each file contains:

- request payload (`question`, `kb_id`, top-k settings)
- generated `graph_context`
- timestamp

Files are auto-created by `api/routes/query.py`.
